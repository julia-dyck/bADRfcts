# 1. write a fit.fgg.model fct with only one input argument (data)

# 2. use it in sim.fit.to.one.sample with lapply -> so more cores can be used parallel
#    -> all priors and pone model in parallel
